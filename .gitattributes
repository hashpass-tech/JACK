# Track PDFs with Git LFS going forward
*.pdf filter=lfs diff=lfs merge=lfs -text
# Binary merge strategy — on conflict, always take theirs (latest)
*.pdf merge=binary
*.png merge=binary
*.ico merge=binary
*.mp4 merge=binary
*.tgz merge=binary
